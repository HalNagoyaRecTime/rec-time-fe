# 環境設定例
NODE_ENV=development

# API設定
VITE_API_BASE_URL=

# 開発サーバー設定
VITE_DEV_PORT=5173
VITE_HOST=0.0.0.0

# HTTPS設定（オプション）
HTTPS_KEY_PATH=./localhost+1-key.pem
HTTPS_CERT_PATH=./localhost+1.pem

# イベント通知設定
# 特定の日付のみ通知する場合は YYYY-MM-DD 形式で指定（例: 2025-10-16）
# 毎日通知する場合は "all" または 空欄 または 未設定
# 例:
#   VITE_EVENT_DATE=2025-10-16  → 2025年10月16日のみ通知
#   VITE_EVENT_DATE=all          → 毎日通知（推奨：テスト用）
#   VITE_EVENT_DATE=             → 毎日通知
VITE_EVENT_DATE=

# 通知タイミング設定（カンマ区切りで複数指定可能）
# - "gather" = グループの集合時間に通知（データベースのf_gather_timeを使用）
# - "start" = イベントの開始時間に通知
# - 数値 = イベント開始時間の指定分数前に通知（例: "30" = 30分前）
# 
# 例:
# "gather,start" = グループ集合時間と開始時間に通知（推奨）
# "gather,30,15,start" = グループ集合時間、30分前、15分前、開始時間に通知
# "15,5" = 15分前、5分前のみ通知
VITE_NOTIFICATION_TIMINGS=gather,start

# メンテナンスモード設定
# true に設定すると、全画面でメンテナンス画面を表示
VITE_MAINTENANCE_MODE=false

# メンテナンス画面に表示するメッセージ
VITE_MAINTENANCE_MESSAGE=メンテナンス中です。しばらくお待ちください。